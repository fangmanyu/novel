version: '3'
services:
  novel_spider:
    build: .
    volumes:
      - ..:/usr/src/app
  redis:
    image: 'redis:alpine'
    ports:
      - '6379:6379'
#  db:
#    image: mysql
#      command: --default-authentication-plugin=mysql_native_password
#      restart: always
#      environment:
#        MYSQL_ROOT_PASSWORD: root